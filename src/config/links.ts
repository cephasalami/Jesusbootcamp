// Central place for primary destination links so they can be updated in one spot.

/**
 * Where every "Join the Boot Camp" / enrollment CTA points.
 * Skool is removed for now — every enrollment CTA routes to the on-site /join
 * email opt-in instead. Point this back at the Skool URL to restore it.
 */
export const JOIN_URL = "/join";
